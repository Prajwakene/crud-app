// recordRoutes.js
const express = require('express');
const router = express.Router();
const RecordController = require('../controllers/RecordController');

// Define record-related routes (create, retrieve, update, delete)

module.exports = router;
